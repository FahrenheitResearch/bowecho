[package]
name = "arwen-map"
version = "0.1.0"
edition.workspace = true
license = "Apache-2.0"
description = "Map foundation: tile basemap (via BowEcho ui_core), vector basemap data, place/coordinate search, Lambert domain math."

[dependencies]
eframe.workspace = true
serde.workspace = true
ui_core.workspace = true
