[package]
name = "arwen-plan"
version = "0.1.0"
edition.workspace = true
license = "Apache-2.0"
description = "gpuwm run-plan contract types: versioned plan JSON, event envelope, run-progress heartbeat. The ONLY crate that touches gpuwm's contract."

[dependencies]
chrono.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
thiserror.workspace = true
toml.workspace = true
