// M0: the distinct exe name comes from `[[bin]] name = "miniderecho"` and
// the window title is set at eframe boot ("miniDerecho"). Icon/VERSIONINFO
// embedding is an M4 task (docs/miniderecho-spec.md §11), mirroring
// app_ui/build.rs's env-var pattern with `MINIDERECHO_*` variables.
fn main() {}
